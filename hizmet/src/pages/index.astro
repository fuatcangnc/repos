---
import Home from '../components/pages/Home.astro';

// Rendered on demand so Astro's i18n middleware can enforce routing:
// with `prefixDefaultLocale: true` unprefixed URLs are redirected/404ed,
// with `prefixDefaultLocale: false` this serves the default locale.
export const prerender = false;
---

<Home />
